# 引き継ぎ: サムネイルのキャラクターを作る

2026年8月18日時点の記録。**次のセッションはここから読んでください。**
`_` 始まりなので GitHub Pages には公開されません。

まず `CLAUDE.md` を読むこと。この文書は**サムネイルのキャラクターに絞った**申し送りです。

---

## 1. キャラクターは入りました（2026/8/18）

**5人 × ポーズ5種の25枚を `_assets/character/cast/` に置いてあります。**
全26記事のサムネイルに反映済みです。作った手順は次のとおり。

1. 運営者の手元の ComfyUI（RTX 3070 Ti / JANKU v7.77）で25枚生成（420秒）
2. 候補をリポジトリ経由で受け取り、`rembg` で背景除去（床の影と脚の間の白も抜ける）
3. `_prepare_character.py` でトリム・縮小して `cast/` に配置
4. `_render_thumbs.py` で全記事を撮り直し、目視確認

**残っている課題は「顔の統一」です。** 素の txt2img なのでポーズごとに顔が少し違います。
IPAdapter の準備（ノード・モデル）は実機で確認済みで、揃え直しは**コマンド1つ**です
（ワークフローは `_comfy_character.py` が組み立てるので、画面で組む作業はありません）。

```bash
python3 _comfy_character.py --face --url http://127.0.0.1:8001
```

基準の顔は採用済みの `cast/<キャラ>/wave.png` が自動で使われます。効きは `--weight`
（既定0.8）。詳しくは `_assets/character/comfy/README.md` の第2節。

**実機で1回失敗しています**: 透過PNGを基準に渡したら縞模様の破綻画像が出ました。
いまは基準を白背景に合成してから送るよう修正済み（手元に Pillow が要ります）。

**プロンプトは 2026/8/18 に厳密化しました**（運営者の指示）。狙いは3つ:
影を背景に出さない / 地面と背景の柄を出さない / キャラの特徴を書き切って生成ごとのぶれを減らす。
同日に背景色も白→緑一色に変わっています（白い服と囲まれた背景が同色で分離できないため）。
何をどう変えたか・タグの裏取り（Danbooru実在タグとの照合）は `recipe.json` のコメント
（`_背景について` / `_影と床について` / `_キャラを固定するために`）に全部書いてあります。
**cast/ の採用25枚は旧プロンプト産**なので、いまの recipe で同じ seed を回しても
同じ絵は出ません（旧版が要るときは git の履歴から `recipe.json` の旧版を取り出す）。

以下は、そこに至るまでの経緯と道具の説明です。

| ある | 中身 |
|---|---|
| `_prepare_character.py` | 取り込み。透過チェック・余白トリム・縮小・`--matte`（単色背景を抜く） |
| `_comfy_character.py` | 手元の ComfyUI に投げて候補を受け取る（キャラ × ポーズを一括） |
| `_assets/character/comfy/recipe.json` | 生成の設定。カテゴリごとの担当キャラ5人とポーズ5種 |
| `_assets/character/comfy/README.md` | IPAdapter で顔を揃える手順（**未実行**。配布元READMEに基づく） |
| `_assets/character/README.md` | 置き方・用意のしかた（ComfyUI / VRoid / 依頼） |
| `_labs/2026-08-18_comfy-character/` | 偽サーバー相手の通し実行の記録 |

```bash
python3 _comfy_character.py --check                # ComfyUI につながるか
python3 _comfy_character.py                        # cast 全員 × ポーズ全種を作る
python3 _prepare_character.py <画像> --cast hinata --as wave --matte   # 取り込む
python3 _prepare_character.py --check              # いまの状態
```

置き場所は次のとおり。`_render_thumbs.py` は**画像があれば読むだけ**で、コード変更は要りません。

```
_assets/character/cast/<キャラ名>/<ポーズ名>.png   # カテゴリごとに担当を決める形(いまはこれ)
_assets/character/default.png                    # 1枚だけ置く形(従来。cast が優先)
```

- 運営者の希望は「**キャラを複数固定して、毎回ポーズや衣装を替えたい**」。
  カテゴリごとに担当を1人ずつ決め、ポーズは記事の日付で選ぶ形にした
- 好みの順は 金髪ツインテール > 茶髪ボブ > 銀髪クール > 黒髪ロング（本人の回答）。
  5人目（チームで作る技術＝黄色テーマ）は、背景に埋もれない濃い青髪にしてある
- 顔の一致は **IPAdapter を使う方針**（本人の選択）。手順は comfy/README.md

- 収まる箱は **幅440px × 高さ660px**（`CHARA_W` / `CHARA_H`）。
  縦横比を保ったまま収まり、占めた幅のぶんだけ文字が狭まります
- **描画時に上から42%だけ切り出します**（`CHARA_CROP`）。全身のまま縮めると
  顔が10px程度になり表情が分からないため。元画像は全身のまま残してあります
- 8/18 に、縦横比を見ずに幅だけ固定していた不具合を直しました。
  縦長すぎる画像で**頭が画面の上端に切られていた**ためです
- 画像が無い間はキャラ無しで組み、文字を広く使います

動作確認の方法: `bash _labs/2026-08-18_comfy-character/run_all.sh`
（ダミーで一通り動かし、最後に片付けて元に戻すところまでやります）

---

## 2. すでに試して**ダメだった**方法（繰り返さないこと）

### 2-1. SVGで手描きする → 不可

2回作って2回とも却下されています。

| 試作 | 結果 |
|---|---|
| ロボット風マスコット（SVG） | 「キャラクターについては、2次元の美少女でお願いします」 |
| アニメ調の少女（SVG） | 「ありえないくらい可愛くないです。一般的なアニメ作品と同等のクオリティで」 |

**手書きのSVGでアニメ作品と同等の品質は出ません。** 調整不足ではなく手法の限界です。
座標でベジェ曲線を並べる方法では、髪・目のハイライト・陰影の情報量が根本的に足りません。
同じ方向で粘らないこと。

### 2-2. この環境で画像を生成することはできない

GPUがありません（`nvidia-smi` 無し、`/dev/nvidia*` 無し。CPU 4コア / メモリ15GB /
書き込み可能ディスク30GB）。CPUだけでアニメ系のSDXL派生を回すと1枚に数分〜十数分かかり、
チェックポイントだけで6GB前後を毎回ダウンロードし直すことになります。
しかも**モデルは公開リポジトリにコミットできません。**

**日次の自動生成に組み込むのも反対**という結論のままです。理由:

- マスコットは毎回同じ顔である必要がある
- 朝5時の無人実行で絵が確定してしまい、誰も確認しないまま公開される

→ 8/18 の結論: **ComfyUI は手元のPCで人が走らせ、出てきた候補から人が1枚選ぶ。**
生成の設定（モデル名・プロンプト・seed）だけをリポジトリに置いて再現性を持たせ、
日次処理は置いてある画像を読むだけにする（第1節の `_comfy_character.py`）。

### 2-3. 参考チャンネルのキャラクターを流用 → 不可

他者の著作物です。`CLAUDE.md` 第6節にも禁止として明記済み。

---

## 3. 次に試す価値がある方向

### 3-1. VRoid Studio（無料・いちばん有望）

**読者＝サイト運営者は VRChat のワールドを作る人**なので、VRM まわりに馴染みがあるはずです。

- 無料でアニメ調のキャラクターを作れる
- 表情差分・ポーズ違いを**いくらでも書き出せる**（背景透過PNG可）
- 一度作れば同じ顔が何度でも出せる ＝ マスコットの要件を満たす
- 自作なので権利が明確

サムネイルには**表情3〜5種 × カテゴリ5色**あれば十分です。

### 3-2. 画像生成AIで作ってもらう（ComfyUI は連携済み）

運営者自身が ComfyUI / NanoBanana / NovelAI / Midjourney / ChatGPT などで作り、
気に入った1枚を選んで置く。**選ぶ工程を人が持てる**のが利点。

ComfyUI については 8/18 に連携を書きました（`_comfy_character.py`）。
手元で ComfyUI を起動して `--check` → 引数なし実行で、候補が
`_assets/character/_candidates/<キャラ名>/<ポーズ名>.png` に落ちてきます。**本物の ComfyUI ではまだ動かしていません**（この環境にGPUが無いため、
APIの形だけをソースで確認し、偽サーバー相手に通しで検証しています）。
最初に動かすときは、まず `--check` でモデル名が合っているかを見てください。

背景は `--matte` で抜けますが、**髪の輪郭に薄く縁が残ります。**
rembg などの背景除去ノードを入れているなら、そちらで透過PNGにしてから取り込むほうが綺麗です。

### 3-3. 絵師に依頼

ココナラ / Skeb など。一度の費用で権利が明確なものが手に入る。

### 3-4. 有償フォントは費用対効果が悪い（調査済み）

「フォントを変えれば近づくのでは」は**検証して否定されています**。
Google Fonts の日本語68書体を全部洗い出し、重い候補8つを並べて比較した結果、
**いま使っている Noto Sans JP Black が無料では最良クラス**でした。

- Mochiy Pop One / Zen Kaku Gothic New 900 / Zen Kaku Gothic Antique 900 … ほぼ差がない
- Reggae One / Potta One … 癖が強く技術記事に合わない
- Train One / Darumadrop One … 装飾的・細い

市販フォント（Adobe Fonts、モリサワ、フォントワークス）は月額数千円〜。
**ただしこのリポジトリは公開なので、フォントファイルをコミットするとライセンス違反**に
なるものがほとんどです。使うなら「フォントは repo に入れず、生成したPNGだけコミット」の構成にすること。

---

## 4. 参考にしているチャンネル

**まさおAIじっくり解説ch**（`https://www.youtube.com/@ai_masaou`）

`curl` で取得できます（ブラウザからは到達不可）。`ytInitialData` に `videoId` があり、
サムネは `https://i.ytimg.com/vi/<id>/sddefault.jpg`（`maxresdefault` は404になることが多い）。

観察した構造（**レイアウトの参考であって、イラストの流用は不可**）:

| 段 | 中身 |
|---|---|
| 1 | 短いフック（黄色＋黒の極太縁）「これからは対話!!」 |
| 2 | 白いカードに製品ロゴ＋製品名 |
| 3 | 巨大な決め文句。**「使ってみた」「徹底解説」「解説します」など4〜5文字の固定フレーズ** |
| 右 | **キャラクター（全サムネに有り。右の3〜4割を占める）** |

**重要**: 巨大文字は記事タイトルではなく短い固定フレーズです。だから極端に大きくできます。
一覧で見ると、記事の内容は「白いカード」が担っています。

---

## 5. いまのサムネイルの作り

`_render_thumbs.py`。1280×720（YouTubeと同じ16:9）。

- カテゴリごとに色（青／マゼンタ／オレンジ／緑／黄）
- 背景の柄は**日付で決まる**（6種×4段階、連続する日は必ず別の柄。乱数は使わない）
- 文字はタイトルから機械的に作る。長すぎるときだけ `meta.json` の `thumb` で上書き
- `og:image` は `_build_index.py` が各記事のサムネイルに書き換える

### ハマったところ（実測メモ・再発防止）

| 現象 | 原因と対処 |
|---|---|
| 撮影すると下が欠ける | `--window-size` は**ウィンドウ枠のぶん(この環境で87px)を引いた大きさ**がビューポートになる。`measure_offset()` で実測して足し、上から切り出している |
| 撮影範囲がビューポートで切れる | `html,body` に `overflow:hidden` を付けると full-page 撮影が効かない |
| 文字が潰れる | `background-clip:text` のグラデ塗りと**太い `-webkit-text-stroke` は併用不可**。塗りは単色に留める |
| 「フック」が「フ/ック」で割れる | `overflow-wrap:anywhere` が原因。`word-break:auto-phrase` + `line-break:strict` を使う |
| 文字が小さく見積もられる | `size_for()` の `lh` を CSS の `line-height` と必ず一致させる |
| 黄・橙テーマで文字が背景に溶ける | 縁の色に `chip` をそのまま使わない。黒を混ぜて必ず背景より暗くする |
| 上下の黒帯＋色枠がボタンに見える | 運営者の指摘で 8/18 に全部外した。**額縁で締める方向はやらない** |
| 文字の下端が毎回数px切られる | 行間1.05 + `overflow:hidden` の組み合わせが原因。padding をはみ出し側に確保した(黒帯があった頃は帯に隠れて見えなかった) |
| 文字の発光がぼやけて見える | `drop-shadow(0 0 Npx)` の発光は外した(運営者の指摘)。白フチと落ち影だけ残す |
| 切り抜きで囲まれた背景が残る | 髪と肩の間の白は**色では消せない**(白い服と同色。実測)。背景除去は birefnet-general を使う(_labs/2026-08-18_comfy-character/) |

### 差し戻した経緯

`5d301f1` で参考チャンネルに寄せる変更（書体変更・3層の縁・暗幕・決め文句の1行切り出し）を
入れましたが、**可読性を落とす方向にも振れてまとまりを欠いた**ため `4babae7` で
`70fba34` の状態に戻しました。16:9 だけ残しています。

同じ方向に再挑戦するなら、**先にキャラクター画像を用意してから**にすること。
文字だけで参考チャンネルの密度に近づけようとすると、今回と同じ失敗を繰り返します。

---

## 6. 現在の状態

- 全26号、サムネイルは 1280×720、内容の重複なし、見出しの省略なし
- 表示検証（幅380px / 900px）で問題なし
- **キャラクター25枚を導入済み**（5人 × ポーズ5種）。全記事のサムネイルに反映済み
- 候補PNG（`_candidates/`）はリポジトリに残していません。`recipe.json` の seed が
  固定なので、同じコマンドで同じ25枚を再現できます
