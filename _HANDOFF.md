# 引き継ぎ: サムネイルのキャラクターを作る

2026年8月18日時点の記録。**次のセッションはここから読んでください。**
`_` 始まりなので GitHub Pages には公開されません。

まず `CLAUDE.md` を読むこと。この文書は**サムネイルのキャラクターに絞った**申し送りです。

---

## 1. 次にやりたいこと

**サムネイルの右側に置くキャラクターを用意する。**

`_render_thumbs.py` には**画像を置くだけで右に入る仕組みがすでにあります**（動作確認済み）。
必要なのは画像そのものだけで、コード側の対応は不要です。

```
_assets/character/default.png          # 全カテゴリ共通
_assets/character/クライアント技術.png   # カテゴリ別(こちらが優先)
```

- 背景透過のPNG、縦長（600×840px 程度以上）
- 置くと右端に配置され、**文字の幅は自動で狭まります**（`CHARA_W = 268`）
- 画像が無い間はキャラ無しで組み、文字を広く使います

動作確認の方法: Pillow でダミーの透過PNGを作って `_assets/character/default.png` に置き、
`python3 _render_thumbs.py <記事名>` して配置と文字幅の追従を見る。確認後は必ず消すこと。

---

## 2. すでに試して**ダメだった**方法（繰り返さないこと）

### 2-1. SVGで手描きする → 不可

2回作って2回とも却下されています。

| 試作 | 結果 |
|---|---|
| ロボット風マスコット（SVG） | 「キャラクターについては、2次元の美少女でお願いします」 |
| アニメ調の少女（SVG） | 「ありえないくらい可愛くないです。一般的なアニメ作品と同等のクオリティで」 |

**手書きのSVGでアニメ作品と同等の品質は出ません。** 調整不足ではなく手法の限界です。
座標でベジェ曲線を並べる方法では、髪・目のハイライト・陰影の情報量が根本的に足りません。
同じ方向で粘らないこと。

### 2-2. この環境に画像生成の手段は無い

画像生成ツールは使えません。`curl` で外部APIを叩くことは技術的には可能ですが、
**日次の自動生成に組み込むのは反対**という結論になっています。理由:

- マスコットは毎回同じ顔である必要がある。画像生成は同じプロンプトでも毎回違う絵を出す
- 朝5時の無人実行で絵が確定してしまい、誰も確認しないまま公開される

### 2-3. 参考チャンネルのキャラクターを流用 → 不可

他者の著作物です。`CLAUDE.md` 第6節にも禁止として明記済み。

---

## 3. 次に試す価値がある方向

### 3-1. VRoid Studio（無料・いちばん有望）

**読者＝サイト運営者は VRChat のワールドを作る人**なので、VRM まわりに馴染みがあるはずです。

- 無料でアニメ調のキャラクターを作れる
- 表情差分・ポーズ違いを**いくらでも書き出せる**（背景透過PNG可）
- 一度作れば同じ顔が何度でも出せる ＝ マスコットの要件を満たす
- 自作なので権利が明確

サムネイルには**表情3〜5種 × カテゴリ5色**あれば十分です。

### 3-2. 画像生成AIで作ってもらう

運営者自身が NanoBanana / NovelAI / Midjourney / ChatGPT などで作り、
気に入った1枚を選んで置く。**選ぶ工程を人が持てる**のが利点。

### 3-3. 絵師に依頼

ココナラ / Skeb など。一度の費用で権利が明確なものが手に入る。

### 3-4. 有償フォントは費用対効果が悪い（調査済み）

「フォントを変えれば近づくのでは」は**検証して否定されています**。
Google Fonts の日本語68書体を全部洗い出し、重い候補8つを並べて比較した結果、
**いま使っている Noto Sans JP Black が無料では最良クラス**でした。

- Mochiy Pop One / Zen Kaku Gothic New 900 / Zen Kaku Gothic Antique 900 … ほぼ差がない
- Reggae One / Potta One … 癖が強く技術記事に合わない
- Train One / Darumadrop One … 装飾的・細い

市販フォント（Adobe Fonts、モリサワ、フォントワークス）は月額数千円〜。
**ただしこのリポジトリは公開なので、フォントファイルをコミットするとライセンス違反**に
なるものがほとんどです。使うなら「フォントは repo に入れず、生成したPNGだけコミット」の構成にすること。

---

## 4. 参考にしているチャンネル

**まさおAIじっくり解説ch**（`https://www.youtube.com/@ai_masaou`）

`curl` で取得できます（ブラウザからは到達不可）。`ytInitialData` に `videoId` があり、
サムネは `https://i.ytimg.com/vi/<id>/sddefault.jpg`（`maxresdefault` は404になることが多い）。

観察した構造（**レイアウトの参考であって、イラストの流用は不可**）:

| 段 | 中身 |
|---|---|
| 1 | 短いフック（黄色＋黒の極太縁）「これからは対話!!」 |
| 2 | 白いカードに製品ロゴ＋製品名 |
| 3 | 巨大な決め文句。**「使ってみた」「徹底解説」「解説します」など4〜5文字の固定フレーズ** |
| 右 | **キャラクター（全サムネに有り。右の3〜4割を占める）** |

**重要**: 巨大文字は記事タイトルではなく短い固定フレーズです。だから極端に大きくできます。
一覧で見ると、記事の内容は「白いカード」が担っています。

---

## 5. いまのサムネイルの作り

`_render_thumbs.py`。1280×720（YouTubeと同じ16:9）。

- カテゴリごとに色（青／マゼンタ／オレンジ／緑／黄）
- 背景の柄は**日付で決まる**（6種×4段階、連続する日は必ず別の柄。乱数は使わない）
- 文字はタイトルから機械的に作る。長すぎるときだけ `meta.json` の `thumb` で上書き
- `og:image` は `_build_index.py` が各記事のサムネイルに書き換える

### ハマったところ（実測メモ・再発防止）

| 現象 | 原因と対処 |
|---|---|
| 撮影すると下が欠ける | `--window-size` は**ウィンドウ枠のぶん(この環境で87px)を引いた大きさ**がビューポートになる。`measure_offset()` で実測して足し、上から切り出している |
| 撮影範囲がビューポートで切れる | `html,body` に `overflow:hidden` を付けると full-page 撮影が効かない |
| 文字が潰れる | `background-clip:text` のグラデ塗りと**太い `-webkit-text-stroke` は併用不可**。塗りは単色に留める |
| 「フック」が「フ/ック」で割れる | `overflow-wrap:anywhere` が原因。`word-break:auto-phrase` + `line-break:strict` を使う |
| 文字が小さく見積もられる | `size_for()` の `lh` を CSS の `line-height` と必ず一致させる |
| 黄・橙テーマで文字が背景に溶ける | 縁の色に `chip` をそのまま使わない。黒を混ぜて必ず背景より暗くする |

### 差し戻した経緯

`5d301f1` で参考チャンネルに寄せる変更（書体変更・3層の縁・暗幕・決め文句の1行切り出し）を
入れましたが、**可読性を落とす方向にも振れてまとまりを欠いた**ため `4babae7` で
`70fba34` の状態に戻しました。16:9 だけ残しています。

同じ方向に再挑戦するなら、**先にキャラクター画像を用意してから**にすること。
文字だけで参考チャンネルの密度に近づけようとすると、今回と同じ失敗を繰り返します。

---

## 6. 現在の状態

- 全25号、サムネイルは 1280×720、内容の重複なし、見出しの省略なし
- 表示検証（幅380px / 900px、52通り）で問題なし
- `main` は push 済み（`4babae7`）
