# このリポジトリで記事を書くときの決まり

このサイトの記事を作成・更新するときは、以下に必ず従ってください。
定期実行タスク（毎朝のダイジェスト、週次の連載）にも同じく適用されます。

---

## 1. 嘘を書かないための規律（最優先）

**読んでいない資料は引用しない。** 検索結果の要約スニペットだけを根拠に
「◯◯にはこう書いてある」と書くのは禁止です。

1. 根拠にする資料は、**必ず本文を取得して該当箇所を自分で読む**
   `WebFetch` がegress制限で失敗したら `curl` で取得し、該当箇所を抜き出して確認する
2. 読めなかったものは引用も要約もしない。言及が必要なら「未確認」「書誌のみ照合」と明記する
3. **一次資料を優先する。** 仕様（RFC / WHATWG / W3C）→ 公式ドキュメント → 公式ブログ →
   原論文 の順。まとめ記事・SEOブログ・AI生成記事は根拠にしない
4. 書誌情報は照合できるなら照合する
   - DOIがある論文: `https://api.crossref.org/works/<DOI>` で著者・誌名・ページを確認
   - 書籍: `https://openlibrary.org/api/books?bibkeys=ISBN:<ISBN>&format=json&jscmd=data`
5. 自分の測定結果と、資料に書いてあることは**分けて書く**
6. 数値・バージョン番号・日付は、必ず出どころを確認してから書く。確認できないものは書かない

### 記事末尾に参考文献セクションを必ず置く

```html
<section class="section section-d" id="refs">
  <h2>参考文献</h2>
  <article class="item">
    <p class="lead">この記事の主張は、次の資料と自分の実測だけに基づいています。<mark>読んでいない資料は引用しません。</mark></p>
    <ol class="refs">
      <li>
        <span class="flag primary">一次資料</span>
        著者名「タイトル」<i>媒体名・規格番号</i>, 発行年月.
        <span class="note">参照箇所: 章節番号と、何を根拠にしたか。</span>
        <a href="URL">URL</a>（YYYY年M月D日参照）
      </li>
      <li>
        <span class="flag unverified">未確認</span>
        …（回答の付いていないフォーラム投稿、本文を読めなかった有料論文など。なぜ未確認かを書く）
      </li>
      <li>
        <span class="flag self">自分の実測</span>
        …（環境・日時・測り方。「加工せずそのまま貼った」ことも書く）
      </li>
      <li>
        <span class="flag self">自分の意見</span>
        …（標準や規格ではなく筆者の運用上の目安であることを明記）
      </li>
    </ol>
  </article>
</section>
```

ラベルは3種類だけ: `primary`（一次資料）/ `unverified`（未確認・書誌のみ照合）/ `self`（自分の実測・自分の意見）。
目次（`.toc`）にも「参考文献」の行を足すこと。

---

## 2. 読ませる工夫は「装飾」ではなく「中身」で作る

読者は集中が続かないタイプです。ただし**飾りを足しても読まれません。**
次のやり方で引き込みます。

- **仕様や公式ドキュメントの原文を、実測結果のすぐ隣に貼る。**
  「実際にこうなった → 規格にこう書いてある」の並びが一番効く
- **見出しに結論を書く。**「〜について」ではなく「〜しても1日も縮まない」
- **意外な事実を早く出す。** 引っ張らない
- **失敗と事故を正直に書く。** うまくいかなかったログが一番読まれる
- **1段落3行以内。1文を短く**
- クイズ・チェックリスト・`next-up` は使ってよい（内容と結びついているため）

### 禁止（ノイズなので入れない）

- `<div class="stats">` のような**大きな数字を並べるだけのブロック**。`data-count` 属性も使わない
- 優先度やタグの意味を説明する文章
- 見出しの先頭に付ける `B.` のような記号
- 記事HTMLへの `<style>` の直書き
- 本文へのインラインSVG（図版はPNGのみ）

---

## 3. サイト構成

トップページ `index.html` は `_build_index.py` が自動生成します。**直接編集しないこと。**

```
index.html            # 自動生成
css/style.css         # ホーム用
css/article.css       # 記事共通 ← 記事HTMLはこれを読み込むだけ
css/shared.css        # 共有バー(自動注入)
js/home.js            # 検索・タグ絞り込み
js/article.js         # 記事の仕掛け(自動注入。記事側で読み込み不要)
contents/YYYY-MM-DD_スラッグ/
    index.html / meta.json / images/ / _figures/
_templates/home.html  # ホームのテンプレート
_build_index.py       # サイトビルダー
_render_figures.py    # SVG → PNG(ライト/ダーク)
_render_thumbs.py     # 記事のサムネイル(1200x630)
_fetch_popular.py     # GoatCounterから閲覧数取得
```

`_` 始まりはGitHub Pages（Jekyll）の公開対象外です。

### 使えるクラス（css/article.css）

| 用途 | 書き方 |
|---|---|
| 本文 | `<p class="lead">`、強調は `<mark>` |
| 要点 | `<ul class="points"><li><b>見出し。</b>説明</li></ul>` |
| コード | `<div class="code"><div class="code-head">名前</div><pre>…</pre></div>` |
| 実行結果 | `<div class="code term">` ＋ `<span class="ok-line">` / `<span class="ng-line">` |
| 日本語の塊 | `<div class="code wrap">`（折り返す） |
| 表 | `<div class="table"><table>…</table></div>` |
| クイズ | `<div class="quiz">` ＋ `<button class="opt" data-correct="true">` ＋ `<p class="ans" hidden>` |
| チェックリスト | `<ul class="checklist" data-key="一意なキー">` ＋ `<span class="cl-count">0 / 3</span>` |
| 次の記事 | `<a class="next-up">` |

**表は2列目以降の `<td>` に必ず `data-label="列名"` を付けること。**
幅620px以下では見出し行が消えて縦積みになるため、これが無いと何の値か分からなくなります。

`<pre>` 内では `<` を `&lt;`、`>` を `&gt;`、`&` を `&amp;` にエスケープします。

---

## 4. ターミナル出力の幅

**1行を短くすること。** 幅380pxの画面で横スクロールせずに読める長さ
（全角なら20文字程度、半角なら40文字程度）に収める。

長くなる場合は、**スクリプト側の出力形式を先に直してから実行し直す**こと。
出力を手で書き換えるのは禁止（それは嘘になります）。列を減らす、2行に分ける、
ラベルを短くする、で対応します。

### pytest の出力を貼るとき（実測済み）

`export COLUMNS=40` を付けて実行する。セパレータ行（`=== FAILURES ===`）と
進捗行（`[100%]`）がその幅に詰まります。ループの中で回すなら `--tb=no` も足す。

ただし2つ限界があります。**どちらも回避できないので、無理に消さないこと。**

- **40未満を指定しても効きません。** 26 / 30 / 34 を実測しましたが、いずれも80桁に戻ります
- **pytest は幅を「文字数」で数えます。** 全角が表示上2桁を占めることを見ていないので、
  日本語のテスト名だと40を指定しても表示幅は超えます
  （`_________ test_ちょうど3000円は送料無料 __________` は40文字ちょうどで、表示幅50桁）

再現一式: `_labs/2026-08-14_ai-basics/`（`run_all.sh` と `verify_pasted.py`）。

### 幅より内容が優先される場合がある

**出力の長さ自体が記事の主張になっているときは、縮めないこと。**
例: `contents/2026-08-14_test-design/` の「E2Eは標準ライブラリを指す」の節は、
スタックトレースが長く原因から遠いことが論点なので、`--tb=line` で1行に縮めると
記事が成立しません。この場合は幅超過を残し、**なぜ残したかを記事か `_labs/README.md`
に書く**こと。`.code` は自分の中で横スクロールするので、ページ全体は崩れません。

---

## 5. 図版は必ずPNG

1. `contents/<記事>/_figures/名前.svg` を作る。色は直書きせず
   `var(--fg)` `var(--sub)` `var(--new)` `var(--old)` `var(--track)` `var(--b)/--c/--a/--d` を使う
2. `python3 _render_figures.py <記事ディレクトリ名>` でライト用とダーク用のPNGを生成
3. HTMLは `<picture>` で出し分ける

```html
<figure>
  <picture>
    <source srcset="images/名前-dark.png" media="(prefers-color-scheme: dark)">
    <img src="images/名前.png" alt="図が示していること" loading="lazy">
  </picture>
  <figcaption>読み取れることを1行</figcaption>
</figure>
```

生成したPNGは **Read で開いて目視確認**すること（文字のはみ出し・重なりがないか）。
図版は2〜3点。装飾イラストは作らない。

---

## 6. meta.json

```json
{
  "category": "カテゴリ名",
  "tags": ["タグ1", "タグ2", "タグ3"],
  "featured": true,
  "title": "記事のh1と同じ",
  "summary": "3〜4文(120〜200字)",
  "thumbnail": "images/thumb.png"
}
```

- `category`: デイリーダイジェスト / AIで作る技術 / Web開発・インフラ / クライアント技術 / チームで作る技術
- `tags`: 3〜6個。**既存タグを優先して使い回す**（`cat contents/*/meta.json | grep -A8 '"tags"'`）。`|` は使わない

### サムネイル（`images/thumb.png`）

**必ず `_render_thumbs.py` で作ること。**

以前は記事HTMLをそのまま撮っていましたが、**全記事が同じ絵になりました。**
どれも上部はトップバー＋白背景＋見出しなので、ホームのカードでは見分けが付かず、
サムネイルの意味がありませんでした（右下のシェアボタンも写り込んでいました）。

いまはサムネイル専用のデザインを組み立てて描画します。狙いは2つです。

1. **カテゴリごとに色を変える** — 一覧に並べたとき、色だけで種類が分かる
2. **決め文句を1つ、極端に大きく置く** — 縮小して読めるのは1〜2行だけ

| カテゴリ | 色 |
|---|---|
| デイリーダイジェスト | 青 |
| AIで作る技術 | マゼンタ |
| Web開発・インフラ | オレンジ |
| クライアント技術 | 緑 |
| チームで作る技術 | 黄 |

```bash
python3 _render_thumbs.py                    # 全記事
python3 _render_thumbs.py <記事ディレクトリ名>  # 1記事だけ
python3 _render_thumbs.py --stale            # 記事HTMLより古いものだけ
```

**`_build_index.py` の後に走らせること。** ビルドはキャッシュ対策の `?v=` と
`og:image` を全記事に書き込むので、先に作ると全部が `--stale` 扱いになります。

#### 文字はどこから取るか

**タイトルから機械的に作ります。煽り文句を勝手に足しません。**
タイトルが「主題 — 補足」の形なので、そこで割って主題を大きく、補足を小さく置きます。
`tags` に入っている語が主題の中に出てきたら、その部分だけ色を変えます。

長すぎて末尾が `…` で切れるときは、`meta.json` に `thumb` を足して短い決め文句を指定します。
**記事の内容と食い違う文言にしないこと。**

```json
"thumb": { "main": "Unity Web を AWS で配る", "sub": "COOP/COEP/CORP は誰に頼むか" }
```

#### 決まりごと

- **タイトルを直したら必ず撮り直す。** 古いままだと、ホームのカードとSNSのシェア画像に
  だけ昔の見出しが残ります（実際に19枚が古いまま残っていました）
- 生成後は **Read で開いて目視確認**する。文字が切れていないか、想定の記事か
- 1200×630。SNSのカード表示に合わせた比率なので変えない
- フォントは `_assets/fonts/NotoSansJP-Black.ttf`（太字が無いと締まりません）。
  リポジトリに入れてあるので、定期実行でも追加取得は不要です

`og:image` と `twitter:image` は `_build_index.py` が**その記事のサムネイルに書き換えます。**

---

## 7. 公開前の検証（必須）

ヘッドレスブラウザで記事を**幅380pxと900px**で開き、実測する。

- `document.documentElement.scrollWidth - window.innerWidth` が 0
- `.code` と `.table` が画面外にはみ出していない
- `.code pre` に背景色が付いている（CSSが効いている）
- JSエラーが0件（`pageerror` と console error を拾う）
- クイズをクリックすると解説が出る／チェックを入れるとカウントが動く

崩れていたら直してから公開すること。

---

## 8. 公開手順

コミットメッセージは**日本語**で書きます。

```bash
git checkout main && git pull origin main
python3 _render_figures.py <記事ディレクトリ名>
python3 _build_index.py
python3 _render_thumbs.py <記事ディレクトリ名>   # タイトルを直したときも必ず
git add -A && git commit -m "日本語のメッセージ"
git push -u origin main
```

- push が失敗したら 2/4/8/16秒 間隔で最大4回リトライ
- `rejected (fetch first)` なら `git pull origin main --no-edit` してから再push
- **検証必須**: `git ls-remote origin main` と `git rev-parse HEAD` の一致を確認する。
  一致するまで未完了

定期実行タスクは main へ直接pushします（リポジトリ所有者が明示的に許可済み）。
システム側で別の作業ブランチが指定されていても、main へpushしてください。PRは作りません。

---

## 9. 専門エージェントを使う

`.claude/agents/` に、この媒体専用のサブエージェントを置いています。
記事を書く・直すときは、**1人で全部やらずにこれらへ振ってください。**

| エージェント | 役割 |
|---|---|
| `source-hunter` | ブログ・YouTube・X から一次資料(仕様/公式/論文)まで登って読む |
| `hands-on-tester` | この環境で実際に動かし、出力を取って裏付ける |
| `dopamine-writer` | 集中が続かない読者向けに、中身で引き込む本文を書く |
| `article-designer` | 理解を速くする図版だけを作る(SVG→PNG、目視確認まで) |
| `fact-verifier` | 主張を1つずつ敵対的に検証し、裏が取れないものを落とす |
| `beginner-judge` | IT未経験の視点で、どこで脱落するかを specific に指摘 |
| `layout-qa` | 幅380px/900pxで実測し、崩れを直す |
| `editor-in-chief` | 統合し、公開可否を判断する |

### 推奨の流れ

```
出典調査 → 実証 → 執筆 → 図版 → 事実検証 → 初学者判定 → 編集 → 表示検証
```

事実検証は**書いた本人以外**にやらせること。自分で書いたものは自分では疑えません。

### 注意

カスタムエージェントは**セッション起動時にしか登録されません**。
実行中のセッションで `.claude/agents/` にファイルを足しても、そのセッションからは
`agentType` で参照できません。その場合は、プロンプトの中で
「/home/user/ai-news/.claude/agents/<名前>.md を読んで、その役割になりきってください」
と指示すれば同じ効果が得られます。

---

## 10. タイトルの付け方

**無理にキャッチーにしない。** 内容を正しく表す名前を優先します。

読者は「釣られて開く」より「探して見つける」ほうが多いので、
**あとから検索して自分で見つけられる名前**にしてください。

| 良くない | 良い |
|---|---|
| pushしたのに直らない。犯人は10分前のCSS | GitHub PagesでCSSの変更が反映されない原因と対策 |
| WebViewだけ動かない。犯人は Origin: null | WebViewのfetchが失敗する理由 — Origin: null とCORS |
| 一番大きいタスクを速くしても、納期は1日も縮まない | タスク分解とクリティカルパス — どこを縮めると納期が動くか |

- **主題を先頭に置く。** 何の話かが最初の10文字で分かること
- 数字や固有名詞は入れてよい（検索で当たるため）
- 煽り・断定調・「衝撃の」「実は」は使わない
- 副題で補足したいことは `<div class="range">` に書く。h1 は短く保つ

### デイリーダイジェストのタイトル

毎朝の記事は、**定期発行だと一目で分かる形式**にします。

```
AI-news：2026/08/15 — その日を表す短い見出し
```

`meta.json` の `title` / `<h1>` / `<title>` / `<meta property="og:title">` の
**4か所を、一字一句同じ文字列**にしてください（デイリーに限らず全記事）。
`<title>` だけ「AIニュース デイリーダイジェスト 2026年8月15日(土)」のように
別文にするのは禁止です。検索結果とブラウザのタブで見出しが変わってしまい、
あとから探して見つけられなくなります。

連載記事（AIで作る技術・チームで作る技術など）は、この接頭辞を付けません。

---

## 11. 「よく分かる解説」を紹介してよい

裏取りの過程で、**正しくて分かりやすい**解説（記事・動画・ポスト）に出会ったら、
参考文献とは**別枠**で読者に紹介してください。参考文献は「根拠」、こちらは「次に読むと良いもの」です。

```html
<section class="section section-d" id="more">
  <h2>もっと知りたい人へ</h2>
  <article class="item">
    <p class="lead">この記事の根拠ではありませんが、<mark>同じテーマを分かりやすく扱っている資料</mark>です。</p>
    <ul class="reads">
      <li>
        <span class="kind">動画</span>
        <a href="URL">タイトル</a>
        <span class="by">発信者 ・ 公開年月</span>
        <span class="why">どこが良いか。何が分かるか。1〜2文。</span>
      </li>
    </ul>
  </article>
</section>
```

`kind` は `記事` / `動画` / `投稿` / `公式` のいずれか。

**紹介の条件**（全部満たすものだけ）:
1. 中身を実際に見た（動画なら該当箇所まで観た）
2. 記事の一次資料と照らして、**内容に誤りが無い**ことを確認した
3. 誰が書いた・話しているかが分かる
4. なぜ良いのかを自分の言葉で書ける

誤りを含む資料は、たとえ分かりやすくても紹介しないでください。

---

## 12. 検証物はバージョン管理する

記事のために書いた検証コード・テスト・図の元データは、`_labs/<記事のディレクトリ名>/` に置きます。
`_` 始まりなのでGitHub Pagesには公開されませんが、**次回以降に再利用できます。**

```
_labs/2026-08-14_cache-busting/
    README.md        # 何を確かめたか / 動かし方 / 実行環境
    serve.py         # 検証用サーバー
    repro.js         # 再現スクリプト
    output.txt       # 実行結果(記事に貼ったものと同一)
    seq.md           # シーケンス図(Mermaid)
```

置くもの:
- **検証コード・テストコード**（記事に貼ったものと同一）
- **実行結果**（`output.txt` など。記事の記述と突き合わせられるように）
- **フローチャート・シーケンス図・クラス図**（Mermaid記法の `.md` でよい。図版PNGの元にもなる）
- **README.md**（何を確かめたか、動かし方、実行時の環境とバージョン）

記事を修正するとき、まず `_labs/` を見れば**前回と同じ条件で測り直せます**。
数値を書き換えるときは、必ず測り直してから直してください。

---

## 13. 扱う技術スタック（2026年8月15日〜）

読者の実務が変わったため、サーバーサイドの対象を入れ替えます。
**この4つは「超基礎から」書きます。** 読者はいずれも業務で使い始めたばかりです。

| 領域 | 対象 | 前提レベル |
|---|---|---|
| アプリケーション | **Laravel / PHP / Composer** | 業務で使い始めたところ |
| データベース | **TiDB** | 超基礎から。MySQLとの違いを明示する |
| クラウド | **AWS / ECS on Fargate / ALB / CloudFront** | 超基礎から |
| セキュリティ | **AWSのセキュリティサービス全般（IAMを起点に）** | 超基礎から。「IAMがあるのは知っている」程度 |
| IaC | **Terraform** | 超基礎から。**CloudFormationは扱わない** |
| コンテナ | **Docker** | 超基礎から |

引き続き扱うもの: クライアント技術(WebView / Unity Web / VRChat)、チーム運営、
AIで作る技術、ブラウザとWeb標準、CI/CD。

### AWSの扱う範囲

**軸はプロジェクトで使うサービス**です。ECS / Fargate / ALB / CloudFront / IAM /
S3 / VPC / CloudWatch / Secrets Manager / ECR / Route 53 あたりが本線で、
記事も日々のダイジェストもここを厚く扱います。

**ただし、この範囲外でも「影響が大きいもの」は必ず載せてください。**
使っていないサービスの話でも、次に当てはまるなら読者の判断が要ります。

| 載せる | 例 |
|---|---|
| **障害・大規模インシデント** | リージョン規模の障害。使っていないサービス起因でも、依存先が巻き込まれる |
| **値上げ・課金体系の変更** | データ転送やNAT Gatewayなど、どの構成にも効いてくるもの |
| **提供終了・非推奨** | 日付と移行先まで書く |
| **セキュリティ** | 権限昇格、クロスアカウントの穴、公開範囲の既定変更、CVE |
| **全体に効く基盤の変更** | IAMの評価順序、VPCの既定動作、TLS/証明書、リージョン追加 |
| **業界の流れが変わる発表** | 主要マネージドサービスの刷新、AI関連基盤の大きな方針転換 |

逆に、**プロジェクトで使わないサービスの細かな機能追加は書きません。**
判断はCLAUDE.md 第16節と同じで、「読者はこれを読んで何かする必要があるか？」です。

### 「超基礎から」の意味

用語を1つも飛ばさないことです。読者は**その技術を触ったことがありません。**

- 初出の用語には必ず1行の説明を付ける（「タスク定義とは、コンテナの起動設定を書いたJSON」）
- **なぜそれが要るのか**を先に書く。仕組みの説明はそのあと
- 画面やコマンドは、どこを押すか・どこに打つかまで書く
- ただし**内容は薄くしない。** 深い話は `<details>` に逃がして、本線は最短距離で通す

### 当面扱わないもの

- **Node.js / npm / Deno / Bun をテーマにした記事は書かない。** 読者から再指定があるまで見送る
- 道具として使うのは可（Laravelの文脈でのVite、検証用のヘッドレスブラウザ操作など）
- 既存のNode.js記事は消さない。過去の記録として残す

### この環境で何を実際に動かせるか（重要）

第1節の規律（読んでいない資料は引用しない／実行していない出力を載せない）は変わりません。
**動かせないものは「動かしていない」と書いてください。**

| 対象 | 実行できるか | 手段 |
|---|---|---|
| **Laravel / PHP** | ✅ できる | PHP 8.4.19 + Composer。`composer create-project laravel/laravel` が通る。DBは `DB_CONNECTION=sqlite` で完結 |
| **TiDB** | ✅ できる | TiUP 1.17.0 を導入済み。`tiup playground` でローカルにクラスタを起動できる |
| **Docker** | ❌ できない | CLIはあるが**デーモンが動いていない**。コンテナは起動できない。Dockerfileの記述と `docker build` の説明は公式ドキュメントに基づいて書き、実行結果は載せない |
| **Terraform** | ✅ 一部できる | Terraform v1.14.3 を導入済み。`init` / `validate` / **`plan` は本物の認証情報なしでも実出力が取れる**（下記）。`apply` は不可 |
| **AWS(ECS/Fargate/ALB/CloudFront/IAM)** | ❌ できない | 認証情報が無い。**実際のリソース作成・課金の発生する操作は一切しない** |

AWS CLI (aws-cli/1.46.0) は導入済みで、**認証情報が要らない操作なら実行できます。**

```bash
aws ecs register-task-definition --generate-cli-skeleton   # 入力JSONの雛形
aws ecs describe-services help                             # コマンドの仕様
```

雛形やヘルプは実出力として載せてよいですが、**API呼び出しの結果を載せてはいけません**
（呼べないので、載せたらそれは捏造です）。

### Terraformの実測のやり方（認証情報なしで plan まで出せる）

プロバイダにダミーの認証情報と検証スキップを渡すと、`terraform plan` が通ります。
**差分の実出力を記事に載せられる**ので、超基礎の説明と相性が良いです。

```hcl
provider "aws" {
  region                      = "ap-northeast-1"
  access_key                  = "dummy"
  secret_key                  = "dummy"
  skip_credentials_validation = true
  skip_requesting_account_id  = true
}
```

```bash
terraform init      # プロバイダを取得
terraform validate  # 書式と型の検査
terraform plan      # 何が作られるかの差分（ここまで実行できる）
```

- **既存リソースを読む必要がある構成（`data` ソースなど）は plan でも失敗します。** その場合は失敗した事実をそのまま書く
- **`terraform apply` は絶対に実行しない。** 実行していないので、適用後の状態を書かない
- `plan` の出力は長いので、CLAUDE.md 第4節（幅380px）に合わせて必要な行だけ抜く。
  抜いたことは「(中略)」で明示する

### AWSのセキュリティを扱うときの注意

読者は「IAMというものがあるらしい」という段階です。**用語の地図を先に描いてください。**

- IAMユーザー / ロール / ポリシー / 信頼ポリシー の違いを、**何を守るためのものか**から説明する
- ECS on Fargate の文脈では、**タスクロールと実行ロールの違い**が最初の関門になる
- 最小権限の話は、原則論ではなく**具体的なポリシーJSON**で示す
- セキュリティグループとNACLのように「似ていて紛らわしいもの」は必ず対比表にする
- **推測でポリシーを書かない。** 公式ドキュメントのアクション名・条件キーを確認してから書く

### AWS・Docker・Terraformの記事で守ること

1. 構成や手順は**AWS公式ドキュメント**（docs.aws.amazon.com）を一次資料にする
2. 料金に触れるときは公式の料金ページを取得して確認する。「だいたい◯ドル」と書かない
3. **実機で確認していないことを、確認したように書かない。**
   参考文献の `self` ラベルには「AWS上での実行は行っていない」と明記する
4. コンソールの画面遷移を書くときは、公式ドキュメントの手順に沿っていることを示す

---

## 14. 会社をまたぐ連携を扱う（重点テーマ）

読者の現場は、**役割が会社をまたいで分かれています。**

| 担当 | 会社 | 守備範囲 |
|---|---|---|
| クライアント | 読者の会社 | Unity Web、WebView、VRChat |
| インフラ・サーバー | 別会社 | AWS、ECS on Fargate、ALB、CloudFront、Laravel、TiDB |

**この境界をまたぐ話は、単独の技術解説より価値があります。** 片方だけ知っていても解けず、
かつ「誰に聞けばいいか分からない」まま放置されやすいためです。積極的に題材にしてください。

### 扱いたい論点の例

- **配信**: Unity Webのビルドをどこにどうやって置くか。S3 + CloudFront か、ECS上のLaravelから配るか。
  それぞれで何が変わるか（キャッシュ、圧縮の効き方、更新の反映、コスト）
- **必要なヘッダー**: Unity Webのマルチスレッドに要る COOP / COEP / CORP を、
  CloudFront と ALB のどちらでどう付けるか。付けたときに外部リソースが壊れないか
- **CORS**: WebViewやUnity WebからLaravel APIを呼ぶときのオリジン設計。誰がどこに書くか
- **認証**: クライアントとサーバーでトークンをどう受け渡すか。WebViewのCookieの扱い
- **デプロイの順番**: クライアントとサーバーを同時に出せないとき、どちらを先に出すか。
  互換性をどう保つか（APIのバージョニング）
- **切り分け**: 障害が起きたとき、クライアント側かインフラ側かをどう判定するか。
  どのログを、どちらの会社が見られるのか
- **契約と責任分界点**: どこまでが自社で、どこからが相手の会社か。
  「設定を変えてほしい」と依頼するときに何を伝えれば通じるか

### 書くときの注意

1. **両側の言葉を書く。** クライアント側の症状と、インフラ側の設定名を必ず対で示す
   （例:「ゲームが起動しない」↔「CloudFrontのレスポンスヘッダーポリシー」）
2. **依頼の文面を実物として載せる。** 相手の会社に何と伝えれば通じるかを、コピーできる形で書く
3. **責任分界点を図にする。** どこまでが誰の担当かが、言葉だけだと伝わりません
4. 推測で相手側の設定を語らない。AWS側は公式ドキュメントを一次資料にする
   （この環境ではAWSを実行できません。CLAUDE.md 第13節の制約に従うこと）

---

## 15. コメント欄（giscus）

記事の末尾（フッターの前）にコメント欄を置けます。`_build_index.py` が自動で注入するので、
**記事HTMLに書く必要はありません。** ホームには入りません。

保存先は **GitHub Discussions** です。バックエンドは不要で、投稿内容はリポジトリに残るため、
将来サービスを乗り換えても持ち出せます。追跡目的のスクリプトも入りません。

### 有効にする手順

1. リポジトリの Settings → General → Features で **Discussions** にチェック
2. https://github.com/apps/giscus をこのリポジトリにインストール
3. https://giscus.app でリポジトリを入力し、表示された `data-category-id` を控える
4. `site.json` の `comments` を編集する
   - `enabled` を `true`
   - `categoryId` に控えた値を入れる
5. `python3 _build_index.py` を実行して push

`repoId` は取得済み（`R_kgDOT35KIA`）。`categoryId` が空でも動きますが、カテゴリ指定なしになります。

### 制約（読者に伝わるよう本文にも書いてある）

- **投稿には GitHub アカウントが必要です。** 技術記事なので許容範囲ですが、
  誰でも書けるわけではない点は認識しておくこと
- コメントは Discussions に紐づくので、記事のURL（`pathname`）を変えると
  それまでのコメントと繋がりが切れます。**公開後に記事のディレクトリ名を変えないこと**

---

## 16. デイリーダイジェストに載せないもの

**「直りました」だけの項目は書かない。** AIツール（Claude Code / ChatGPT / Copilot /
Cursor / Gemini など）は更新頻度が高く、CHANGELOGを開けば毎日いくつも修正が並びます。
これを拾うと件数は埋まりますが、**読者の時間を使わせるだけで何の判断材料にもなりません。**

### 載せない

- 不具合修正の報告（「〜が直った」「〜の失敗を修正」「表示が崩れる問題を解消」）
- 体感できない内部改善、軽微なUI調整、細かなパフォーマンス改善
- バージョンが上がったこと自体（中身に読者への影響が無いもの）
- 「不調ならまず更新を」で終わる項目

### 載せる（対応が要るもの）

| 種類 | 例 |
|---|---|
| 仕様変更・デフォルトの変更 | 既定の挙動が変わり、既存の自動化やCIが影響を受ける |
| 提供終了・非推奨 | 日付と、移行先まで書く |
| 料金・利用上限の変更 | いつから、いくらになるか |
| **セキュリティ** | 権限バイパス、情報漏洩、CVE。修正であっても**更新の判断が要る**ので載せる |
| 新機能 | 読者が実際に使えるようになったもの |

### 判定のしかた

書く前に **「読者はこれを読んで何かする必要があるか？」** を自問してください。
「へえ、で終わる」なら載せません。

**動きが無い日は無理に埋めないこと。** `<p class="none">特筆すべき動きなし</p>` の1行で構いません。
件数を揃えるために細かい修正を拾うのは、いちばんやってはいけないことです。

---
