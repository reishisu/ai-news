# 【must】数字の訂正（Claudeの誤り。**録画とすべての文面で直すこと**）

**Claudeが渡した「長尺19本・中央値28」は誤りでした。** 19本というのは、
このリポジトリに原稿がある動画だけの一覧（`stats_shape.py` の出力）で、
チャンネルの全長尺ではありませんでした。

`VIEWS_LOG.json`（`track_views.py` が毎日書いている全記録）で数え直した正しい値:

| | 誤（使わない） | **正** |
|---|---|---|
| 長尺の本数 | 19本 | **23本**（記録は24件。うち1件は同じ回の重複で0回） |
| 再生数の中央値 | 28 | **22** |
| 8/23の回（69再生）の順位 | 6位 | **6位**（変わらず） |
| 中央値の何倍か | 2.5倍 | **3.1倍** |

23本の再生数（多い順・実測）:
`392, 233, 91, 85, 83, 69, 47, 47, 46, 28, 24, 22, 19, 15, 11, 10, 9, 9, 7, 7, 6, 4, 4`

## 直す場所
1. **録画**（`codex-exec-result.txt` の中で「中央値28の2.5倍」と言っている）。
   撮り直しのときに、**プロンプト側の数字を「23本中6位・中央値22・3.1倍」に直してから**録ること
2. 記事・`video.json` の該当箇所すべて
3. すでに直っているもの: `out/_codex/ground-truth-2026-09-01.md`（Claudeが更新済み）

**結論は変わりません**（69回は上位1/3で「不発シグナル」ではない）。**数字だけが違います。**
